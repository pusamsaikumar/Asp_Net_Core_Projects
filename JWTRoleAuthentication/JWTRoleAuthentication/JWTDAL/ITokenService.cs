using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;

namespace JWTRoleAuthentication.JWTDAL
{
    public interface ITokenService
    {
       // string GenerateAccessToken(IEnumerable<Claim> claims);
       // string GenerateRefreshToken();
       // ClaimsPrincipal GetPrincipalFromExpiredToken(string token);
       // JwtSecurityToken GenerateJwtToken(string username);
       // JwtSecurityToken GenerateJWTToken(string userName, string email, int storeId, DateTime dataOfBirth, string role);
       // string GenerateJwtAccessToken(string username);
       //string GenerateJwtToken(string userName, string email, int storeId, DateTime dataOfBirth, string role);
        
    }
}
